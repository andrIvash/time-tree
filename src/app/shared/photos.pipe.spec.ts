/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PhotosPipe } from './photos.pipe';

describe('PhotosPipe', () => {
  it('create an instance', () => {
    let pipe = new PhotosPipe();
    expect(pipe).toBeTruthy();
  });
});
